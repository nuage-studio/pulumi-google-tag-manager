# Pulumi project file
# https://www.pulumi.com/docs/intro/concepts/project/

name: pulumi-google-tag-manager
runtime: python
description: A project deployed via Pulumi
# Where __main__.py is located
main: ./pulumi
# Where stack configs are located
config: ./pulumi
# Default config for stacks
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-west-1